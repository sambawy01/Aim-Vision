[build]
# TODO(camera-core): scaffolding code has minor clippy warnings (e.g.
# `.ok().map(|()| ())` patterns in tests). Re-enable warnings-as-errors
# after the first Rust engineer hired runs `cargo fmt --all` and
# `cargo clippy --workspace --fix`. Until then, warnings are warnings.
# rustflags = ["-D", "warnings"]
